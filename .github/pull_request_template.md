## Summary

Provide a concise description of the changes proposed in this Pull Request.

## Changes Proposed
- 

## Security & Architecture Impact
- [ ] No secrets, keys, or real PCAPs are committed.
- [ ] Input validation applied (path traversal, size limits, format checks).
- [ ] Subprocess execution uses safe argument lists (no `shell=True`).
- [ ] API endpoints are versioned (`/api/v1/`).

## Testing & Verification
- [ ] Unit tests pass (`pytest` / `PYTHONPATH=src python3 -m unittest discover tests`).
- [ ] Code formatting & linting verified (`ruff check .`, `npm run lint`).
- [ ] Frontend compiles cleanly (`npm run build`).

## Breaking Changes
- [ ] None (backwards-compatible)
- [ ] Yes (documented below)
